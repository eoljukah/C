#include <stdio.h>

int main()
{
	int num_cat;	 //변수 명은 알파뱃 소문자 대문자,숫자,언더바(_) 가능
	int my_love123;	 //대문자 소문자 구분 해야함
	int MY_LOVE123;
	int x;		
	int _love	

	return 0;    
					 //앞에 언더바 사용가능(초보일때는 앞에 언더바(_) 사용은 자제)
					 //단 숫자를 앞에 사용할 수 없음
					 //변수명의 사이에 빈칸이 있으면 안됨(빈칸을 넣고 싶다면 언더바(_)사용)
					 //한 영역 안에 같은 이름을 갖는 변수가 두개 이상 있으면 안됨
					 //변수명에 특수기호 사용 불가
	
		
}


